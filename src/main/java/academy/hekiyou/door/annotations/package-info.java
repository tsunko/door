/**
 * Provides annotations for use when utilizing door.
 * <p>
 * In essence, we would have classes annotated with {@link academy.hekiyou.door.annotations.Module}, indicating
 * that the command is one containing commands. Within these classes, we would have functions that are annotated with
 * {@link academy.hekiyou.door.annotations.RegisterCommand}.
 */
package academy.hekiyou.door.annotations;